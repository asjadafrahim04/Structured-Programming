#include<stdio.h>
//ASCII number and display appropriate
characters on screen
int main(){
    int ascii;
    scanf("%d",&ascii);

    char ch = ascii;
        printf("%c",ch);
    return 0;
}
